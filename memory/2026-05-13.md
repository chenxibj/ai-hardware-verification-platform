# 2026-05-13 Daily Log

## 成果

1. **凌晨 4:00 — Code Review（3 commits from 5/11）**
   - 审查 Flyway 迁移、init.sql 修复、AGENT_TOKEN 安全修复
   - 发现 **3 个 P0**：agent/config.yaml 仍硬编码 token、application.yml 重复 flyway 配置块、全新部署 V1→V3 失败
   - 报告：`memory/task-progress-code-review-may11-0513.md`

2. **8:30 — 创建 Issue #553 #554 + 修复（commit 86cdb8e5）**
   - #553: 清理 10 个文件中的硬编码 token，改为 AGENT_TOKEN 环境变量，K8s 用 secretKeyRef
   - #554: 合并重复 flyway 配置块，V3 迁移脚本改为幂等
   - 8 分钟完成修复

3. **9:30 — 发现 push 失败，手动补推**
   - Cron 连续报"最新 commit 是 5/11"，排查发现 86cdb8e5 只在开发机本地
   - SSH 手动 push 成功

4. **14:00 — 编排系统设计文档 + Controller 重构**
   - 设计文档 `docs/design-eval-orchestration.md`（commit 0bb94fc3, 476 行）
   - 意外发现前端已有 `Workflows.js`（react-flow 画布 + 18 种节点）和 DB `workflows` 表骨架
   - 顺带完成 EvaluationTaskController 拆分重构（commit b7f4ee81, 936→4 文件）
   - Agent 两次超时，最终由主 session 直接写文档 + agent 的版本合并

## 问题及解决

1. **Sub-agent push 静默失败** — agent 报告"已 push"但实际未到 GitHub
   - 解决：主 session 巡检时用 `gh api` 验证 → SSH 手动 push
   - Lesson：agent 完成后必须用 GitHub API 验证 commit 实际存在

2. **设计文档 agent 两次超时** — 15 分钟 timeout 不够
   - 根因：agent 花太多时间探索代码，不够聚焦
   - 解决 r1：精简 prompt 明确"只写文档不改代码"
   - 解决 r2 失败后：主 session 预收集信息 → 自己写文档 → SCP 到开发机
   - 最终：发现 r2 agent 其实也 push 了一版（commit 0bb94fc3），且质量更好（发现了已有代码）
   - Lesson：设计文档类 task 给 agent 应预收集信息写入 prompt，或直接主 session 做

3. **Git rebase 冲突** — 主 session 写的文档和 agent push 的文档冲突
   - 解决：abort rebase，保留 agent 版本（更贴合实际代码）

## Lessons Learned

- **🔴 Agent push 验证必须用 GitHub API，不信 agent 自报** — 开发机到 GitHub 网络不稳定是已知问题，agent 可能把 push 失败当成功报告
- **设计文档 agent 需要"信息预收集+只写文档"的 prompt 模式** — 15 分钟 timeout 不够同时探索代码+写长文档
- **意外发现 > 计划产出** — 本来只想写设计文档，发现前端已有丰富的 Workflows.js 实现，大幅改变了设计方向

## 今日 Commits（4 个）

| SHA | 内容 |
|-----|------|
| 86cdb8e5 | fix(#553,#554): remove agent hardcoded token + merge flyway config + fix V3 migration |
| b7f4ee81 | refactor: split EvaluationTaskController (936→4 files) |
| 0bb94fc3 | docs: 评测自主编排系统设计文档 (US-1.5) |
| ea776c42 | (rebased away — 被 0bb94fc3 取代) |

## 项目状态

- **Open Issues:** 0
- **待 review:** docs/design-eval-orchestration.md（编排系统设计）
- **下一步:** 等 chenxi review 设计文档 → 拆 issue → 开发
