# 节点 Agent 配置
platform:
  url: "http://localhost:8080/api"
  # Agent 认证 Token（与后端约定）
  token: "ahvp-agent-secret-2026"

node:
  name: "dev-node-01"
  description: "开发测试节点 - CentOS Stream 9, 4C 14GB"
  tags: "cpu,dev,centos"

agent:
  port: 8090
  host: "0.0.0.0"

heartbeat:
  interval: 30  # 秒

# 评测脚本根目录（相对于项目根）
eval_scripts_dir: "/root/ai-hardware-verification-platform/eval-scripts"
project_root: "/root/ai-hardware-verification-platform"
