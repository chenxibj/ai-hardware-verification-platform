# Issue 审查报告 — AI 硬件评测平台第一期 🥬

> 项目：[ai-hardware-verification-platform](https://github.com/chenxibj/ai-hardware-verification-platform)
> 里程碑：第一期（CPU 系统）- 2026.09
> 审查范围：#36-#45（菜菜子细化 Issue）+ #29-#35（早期 Issue）
> 审查日期：2026-03-31

---

## 一、PRD 覆盖度分析

### 1.1 需求追踪矩阵

| PRD 需求模块 | 对应 Issue（活跃） | 覆盖度 | 评判 |
|-------------|-------------------|--------|------|
| **评测任务管理**（创建/调度/监控/终止重试） | #29(closed), #36(创建表单重构), #45(UI/UX含任务详情页) | ⚠️ 部分覆盖 | 创建已细化，但**调度策略、终止/重试、任务队列**缺少独立细化 Issue |
| **评测报告管理**（生成/存储/分享） | #30(closed), #39(报告模板), #42(自动生成+双向关联) | ✅ 基本覆盖 | 报告分享/导出在 #39 有提及 |
| **评测执行引擎**（CPU 上模型推理+算子评测） | #33(closed), #40(执行引擎细化), #38(验收用例) | ✅ 覆盖良好 | 细化到框架选型、算子列表、验收用例 |
| **用户认证体系**（注册/登录/基础权限） | #31(closed) | ⚠️ 部分覆盖 | 早期 #31 已关闭，但**无细化 Issue**，功能点停留在粗粒度 |
| **CPU 资源池管理** | #32(closed), #44(资源管理完善) | ✅ 覆盖良好 | #44 细化到录入/监控/调度/告警 |
| **评测对象管理**（模型/算子注册） | #37(评测对象管理) | ✅ 覆盖 | 含预置模型、算子、框架列表 |
| **数据集管理** | #41(数据集管理) | ✅ 覆盖 | 含预置数据集+用户上传 |
| **评测日志管理** | #40 内含(实时日志 WebSocket) | ⚠️ 隐性覆盖 | 日志作为执行引擎子功能，未独立 Issue |
| **评测结果数据管理** | #43(指标体系定义) | ⚠️ 部分覆盖 | 指标定义有了，但**结果数据的存储、查询、导出**缺独立 Issue |
| **基础告警机制** | #44 内含(资源告警) | ⚠️ 隐性覆盖 | 仅覆盖资源告警，**任务异常告警、系统级告警**缺失 |

### 1.2 覆盖率计算

- **已明确覆盖（✅）**：5/10 = 50%
- **部分/隐性覆盖（⚠️）**：5/10 = 50%
- **完全遗漏（❌）**：0/10 = 0%

**整体评价**：核心模块都有 Issue 触达，但有 5 个模块的覆盖是"嵌套在其他 Issue 里"或"粗粒度未细化"，**需要补充独立的细化 Issue**。

---

## 二、遗漏需求清单（建议新建的 Issue）

### 🔴 P0 — 必须补建

| # | 建议 Issue 标题 | 简要描述 | 理由 |
|---|---------------|---------|------|
| 1 | **【需求细化】评测任务调度与队列管理 — 优先级排序、并发控制、排队策略** | 评测任务提交后如何排队？优先级如何计算？并发限制多少？队列满了怎么办？应包含：任务优先级排序算法、并发任务数量限制、队列溢出策略、调度日志 | #29 和 #36 只覆盖了任务创建，调度策略是独立技术问题 |
| 2 | **【需求细化】评测任务监控与控制 — 状态流转、终止/重试、超时处理** | 任务状态机定义（待调度→排队中→运行中→已完成/失败/已取消），终止运行中任务的API和前端按钮，失败任务手动/自动重试策略，超时自动终止 | PRD 明确要求终止/重试能力，当前无独立 Issue |
| 3 | **【需求细化】用户认证体系完善 — 注册/登录流程、JWT 会话、基础权限控制** | 细化 #31 的功能点：第一期最小集=邮箱注册+密码登录+JWT Token+基础角色（管理员/普通用户），验收标准需量化（如 Token 有效期、密码强度规则等） | #31 已关闭且粒度太粗，缺少细化实现方案 |
| 4 | **【新增】评测日志管理 — 日志采集、存储、查询、清理** | 独立管理评测日志的全生命周期：日志格式规范、日志存储方案（文件/数据库/ES）、日志查询API（按任务ID/时间/级别过滤）、日志保留策略（自动清理30天前日志）、日志下载导出 | 当前日志功能分散在 #40（实时日志）和 #45（前端日志Tab），缺少后端存储管理方案 |
| 5 | **【新增】评测结果数据管理 — 结构化存储、查询、趋势分析** | 评测结果数据的存储方案（结构化JSON + 数据库表）、结果查询API（按任务/模型/时间段查询）、历史趋势对比（同模型不同时间的性能变化）、数据导出（CSV/JSON） | #43 只定义了指标体系，但结果数据如何管理（CRUD、查询、导出）没有覆盖 |

### 🟡 P1 — 建议补建

| # | 建议 Issue 标题 | 简要描述 | 理由 |
|---|---------------|---------|------|
| 6 | **【新增】系统告警机制 — 任务异常告警 + 系统健康告警** | 除 #44 的资源告警外，还需：任务执行失败告警、任务超时告警、系统磁盘满告警、数据库连接异常告警。告警渠道：站内通知（第一期），后续接邮件/钉钉/飞书。告警规则配置 UI | #44 只有资源告警，PRD 要求的"基础告警机制"范围更广 |
| 7 | **【新增】报告分享与权限 — 报告链接分享、访问控制** | 报告分享链接生成（公开/需登录/指定用户可见）、分享链接有效期设置、报告水印、下载权限控制 | #30 功能点提到了报告分享和权限控制，但无细化 Issue |
| 8 | **【技术】数据库设计文档 — 第一期核心表结构定义** | 整理第一期所有实体的数据库表设计：用户表、任务表、报告表、资源表、评测对象表、数据集表、指标表、日志表。含字段定义、索引、外键关系 | 多个 Issue 涉及数据模型但各自为政，需要统一设计 |

---

## 三、#36-#45 Issue 质量审查

### 3.1 总体评价

| 维度 | 评分 | 说明 |
|------|------|------|
| **背景说明** | ⭐⭐⭐⭐ | 每个 Issue 都有背景段落，说明了为什么做 |
| **功能点细化** | ⭐⭐⭐⭐⭐ | checkbox 列表非常详细，大部分可独立验证 |
| **验收标准** | ⭐⭐⭐ | 有验收标准但部分不够量化（见下方详细评价） |
| **技术方案** | ⭐⭐ | 大部分缺少技术实现建议 |
| **依赖关系** | ⭐⭐ | 只标注了关联 Issue 编号，未说明阻塞关系 |
| **INVEST 合规** | ⭐⭐⭐ | 部分 Issue 过大（#44, #45），不满足 S 原则 |

### 3.2 逐 Issue 审查

---

#### #36 — 评测任务创建表单重构

**✅ 优点**
- 分步表单设计清晰（4步），每步功能点明确
- 现状问题分析到位（裸JSON不可用）
- 验收标准有"无需编写JSON"等用户视角标准

**⚠️ 改进建议**
- **验收标准需量化**："3步内完成"较好，但缺少页面加载性能标准（如"表单渲染<1秒"）
- **缺技术方案**：未建议使用哪个表单库（如 Ant Design ProForm StepsForm）、表单状态管理方案
- **缺依赖说明**：Step2 选择评测对象依赖 #37（评测对象管理先完成），Step3 选择资源依赖 #44（资源管理先完成），Step2 数据集选择依赖 #41
- **INVEST-S**：功能点较多（17个 checkbox），建议拆分为"Step1-2 基础表单"和"Step3-4 资源配置+提交"两个 Issue

---

#### #37 — 评测对象管理

**✅ 优点**
- 数据模型设计完整（实体字段、CRUD API）
- 预置评测对象列表具体且合理（CPU 环境下的小模型选型专业）
- 创建任务联动明确

**⚠️ 改进建议**
- **验收标准不够量化**："增删改查评测对象"过于简单，建议增加：API 响应时间<500ms、列表分页支持、搜索响应<1秒
- **缺技术方案**：评测对象的"规格参数（JSON）"字段建议给出 JSON schema 示例
- **缺依赖说明**：#36 的 Step2 依赖本 Issue，应在本 Issue 标注"被 #36 依赖"
- **模型文件存储**：用户上传自有模型的文件存储方案未提及（本地存储？OSS？大小限制？）

---

#### #38 — CPU 验收用例

**✅ 优点**
- 测试用例极其详细（10个Case），参数具体到 shape、精度、迭代次数
- 有预期性能范围（如 "ResNet-50 延迟约 50-200ms/image"）
- 资源不足场景有兜底策略（标注为"待GPU环境验证"）

**⚠️ 改进建议**
- **本 Issue 性质特殊**：这不是一个开发功能 Issue，而是**测试用例文档**，建议打 `type:test-case` 标签以区分
- **验收标准截断**：Issue body 似乎被截断，未看到完整验收标准
- **数据集准备**：Case 7-10 需要数据集，依赖 #41 完成
- **缺少自动化建议**：这些 Case 是手动跑还是自动化？建议增加：CI 集成方案、测试脚本位置约定

---

#### #39 — 评测报告模板

**✅ 优点**
- 报告模板结构化程度极高（按评测类型分模板，指标覆盖全面）
- 区分基础版/高级版，符合商业模式
- 通用头信息完整

**⚠️ 改进建议**
- **INVEST-S 不满足**：这个 Issue 太大（4种模板 × 多个指标类别 + 功能点），建议拆为：
  - #39-a：报告模板框架 + 通用头信息
  - #39-b：模型推理评测报告模板
  - #39-c：算子评测报告模板
  - #39-d：高级版额外功能
- **缺技术方案**：报告渲染用什么技术？（如前端 React 组件 + 后端模板引擎）、PDF 导出用什么库？（如 Puppeteer / wkhtmltopdf）
- **验收标准被截断**：原文未完整展示
- **与 #43 重叠**：#43 定义了指标体系，#39 也列了大量指标定义，两者有重复

---

#### #40 — CPU 评测执行引擎

**✅ 优点**
- 技术方案极其详细：框架选型（ONNX Runtime / PyTorch）、模型格式、推理参数、指标采集方法
- 算子 Benchmark Runner 设计完整
- 异常处理考虑周全（OOM、超时、段错误）

**⚠️ 改进建议**
- **INVEST-S 不满足**：这是最大的 Issue（40+ checkbox），建议拆为：
  - #40-a：模型推理 Runner（ONNX Runtime + PyTorch）
  - #40-b：算子 Benchmark Runner
  - #40-c：评测通用功能（日志、异常处理、进度追踪）
- **缺依赖说明**：依赖 #44 资源分配（需要知道在哪台机器上执行）、依赖 #37 评测对象（需要获取模型/算子信息）、依赖 #41 数据集（需要加载测试数据）
- **验收标准可加强**：当前验收标准偏"功能可用"，建议增加性能标准（如"评测引擎本身的开销 < 评测总时间的5%"）

---

#### #41 — 数据集管理

**✅ 优点**
- 预置数据集选择合理，附带 HuggingFace 来源链接
- 上传限制明确（500MB/文件，5GB/租户）
- 数据预览功能实用

**⚠️ 改进建议**
- **验收标准**："至少预置5个开源数据集"较好，但缺少：数据集列表加载时间、上传大文件的进度条、上传校验时间
- **缺技术方案**：数据集存储在哪？（本地磁盘 / OSS）、如何引用 HuggingFace 数据集（下载到本地？用 datasets 库动态加载？）
- **缺依赖说明**：#36 Step2 和 #38 的验收用例都依赖数据集

---

#### #42 — 评测报告自动生成 + 双向关联

**✅ 优点**
- 需求拆解到位：自动生成、双向关联、手动创建保留、状态流转
- 验收标准可量化："10分钟内生成报告"、"点击跳转"

**⚠️ 改进建议**
- **验收标准加强**：并发场景（10个任务同时完成，报告生成不阻塞）、报告内容正确性（指标数值与原始数据一致）
- **缺技术方案**：自动生成用异步队列（如 Celery / 消息队列）还是同步触发？报告模板渲染引擎选型？
- **缺依赖说明**：依赖 #39（报告模板）和 #43（指标体系）先完成

---

#### #43 — 评测指标体系定义

**✅ 优点**
- 指标定义非常专业和全面（模型推理 20+ 指标、算子 12+ 指标、框架 10+ 指标）
- 指标元数据模型设计合理（含数据类型、单位、适用评测类型）

**⚠️ 改进建议**
- **与 #39 重叠严重**：#39 报告模板里也列了大量指标定义，建议在 #39 中引用 #43 的指标定义，避免两边不一致
- **验收标准过简**："所有预置指标可在系统中查看"缺少量化：指标数量（预期 50+ 个？）、API 性能、管理界面可操作性
- **缺技术方案**：指标存储方案（关系表？JSON？时序数据库？）、指标查询性能要求
- **INVEST-V**：指标定义本身不直接产生用户价值，价值通过报告和结果展示体现——建议标注为 "blocked by: none, blocks: #39, #42"

---

#### #44 — CPU 资源管理完善

**✅ 优点**
- 功能点极细：资源录入表单、实时监控、告警规则、调度对接全覆盖
- 告警规则具体（CPU≥95%持续5分钟、内存≥90%、磁盘≥85%）
- 监控数据采集间隔明确（≤10秒）

**⚠️ 改进建议**
- **INVEST-S 不满足**：5个子模块（录入、监控、告警、调度、维护），30+ checkbox，建议拆为：
  - #44-a：资源录入与基础管理（CRUD + 批量导入）
  - #44-b：资源实时监控（采集 + 仪表盘 + 折线图）
  - #44-c：资源告警与通知
  - #44-d：资源分配与调度对接
- **缺技术方案**：监控采集用什么（SSH + 定时脚本？Prometheus Node Exporter？）、监控数据存储（Prometheus？InfluxDB？PostgreSQL？）、前端图表库（ECharts / Ant Design Charts）
- **依赖关系**：#44-d 的资源分配 API 被 #40 执行引擎依赖，应标注

---

#### #45 — 前端 UI/UX 优化

**✅ 优点**
- 基于实际体验测试提出，问题描述具体
- 涵盖任务列表、详情、报告、全局导航等多个页面

**⚠️ 改进建议**
- **INVEST-S 严重不满足**：这是一个"UI 改进大杂烩"，包含了 25+ 个改进点，跨越了任务管理、报告管理、全局导航等多个模块。建议拆为：
  - #45-a：任务列表筛选与搜索
  - #45-b：任务详情页（含日志Tab、监控Tab）
  - #45-c：报告详情页结构化展示
  - #45-d：全局 UI 改进（面包屑、空状态、loading、toast）
- **验收标准不够量化**："面包屑导航正常工作"应改为"所有页面面包屑层级正确，点击可返回上级"
- **缺优先级子排序**：25 个改进点的内部优先级不同（任务详情页 > 空状态优化），需标注
- **缺技术方案**：WebSocket 实时日志的前端实现方案、CPU/内存实时图表的组件选型

---

## 四、建议的 Issue 依赖关系

```
                    ┌──────────┐
                    │ #31 用户  │ (需细化 → 新 Issue #3)
                    │  认证体系 │
                    └──────┬───┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐       ┌────▼────┐        ┌────▼────┐
   │ #37 评测 │       │ #41 数据 │        │ #43 指标 │
   │ 对象管理 │       │ 集管理  │        │ 体系定义 │
   └────┬────┘       └────┬────┘        └────┬────┘
        │                  │                  │
        └────────┬─────────┘                  │
                 │                            │
           ┌─────▼─────┐                ┌─────▼─────┐
           │ #36 任务   │                │ #39 报告  │
           │ 创建表单   │                │ 模板      │
           └─────┬─────┘                └─────┬─────┘
                 │                            │
      ┌──────────┤                            │
      │          │                            │
 ┌────▼────┐  ┌──▼────────┐            ┌─────▼─────┐
 │ #44 资源 │  │ 新#1 任务  │            │ #42 报告  │
 │ 管理完善 │  │ 调度/队列  │            │ 自动生成  │
 └────┬────┘  └──┬────────┘            └───────────┘
      │          │
      └────┬─────┘
           │
     ┌─────▼─────┐
     │ #40 评测   │
     │ 执行引擎   │
     └─────┬─────┘
           │
     ┌─────▼─────┐
     │ #38 验收   │
     │ 用例跑通   │
     └───────────┘
```

### 建议的执行顺序（Sprint 规划参考）

| Sprint | 周期 | Issue | 说明 |
|--------|------|-------|------|
| Sprint 1 | Week 1-3 | 新#3(用户认证细化), #37(评测对象), #43(指标体系) | 基础数据层，无依赖 |
| Sprint 2 | Week 3-5 | #41(数据集), #44-a(资源录入), #39(报告模板) | 依赖 Sprint 1 |
| Sprint 3 | Week 5-8 | #36(任务创建), #44-b(资源监控), 新#1(任务调度) | 核心业务流 |
| Sprint 4 | Week 8-11 | #40(执行引擎), #44-c/d(告警+调度对接) | 执行层 |
| Sprint 5 | Week 11-13 | #42(报告自动生成), 新#2(任务监控控制), 新#4(日志管理) | 联调+数据流 |
| Sprint 6 | Week 13-15 | #38(验收用例), #45(UI优化), 新#5(结果数据管理) | 测试+打磨 |
| Buffer | Week 15-16 | Bug修复、联调、性能优化 | 缓冲期 |

---

## 五、总结与建议

### 做得好的地方 👍

1. **#36-#45 的功能点细化程度很高**，checkbox 列表详细且大部分可独立验证
2. **评测指标体系（#43）非常专业**，覆盖了行业标准指标
3. **验收用例（#38）具体到输入参数和预期性能**，这在很多项目里是缺失的
4. **所有 Issue 都有背景说明和优先级标注**，上下文清晰

### 需要改进的地方 🔧

1. **Issue 粒度控制**：#39, #40, #44, #45 都太大，单个 Issue 30+ checkbox，不满足 INVEST 的 S（Small）原则，应拆分
2. **依赖关系缺失**：大部分 Issue 只写了"关联 #XX"，没有区分"依赖"（blocked by）和"被依赖"（blocks），不利于排期
3. **技术方案建议缺失**：10 个 Issue 中仅 #40 有较完整的技术选型，其他 Issue 缺少实现方向建议，可能导致开发理解偏差
4. **验收标准量化不足**：部分验收标准停留在"功能可用"层面，缺少性能/安全/异常场景标准
5. **#39 和 #43 存在重叠**：指标定义出现了两次，需要明确 #43 为指标定义的 Single Source of Truth，#39 引用 #43

### 下一步行动建议

- [ ] 补建上述 5 个 P0 Issue + 3 个 P1 Issue
- [ ] 将 #39, #40, #44, #45 拆分为更小的子 Issue
- [ ] 为所有 Issue 补充依赖关系说明（用 GitHub 的 "blocked by" 功能或在 body 中标注）
- [ ] 为关键 Issue 补充技术方案建议段落
- [ ] 修复 #39 和 #43 的指标重叠问题

---

_审查人：菜菜子分析助手 🥬 | 2026-03-31_
